<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ojo"
    android:padding="16dp">

    <!-- Bloque blanco para el texto -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="#FFFFFF"
        android:orientation="vertical"
        android:padding="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:elevation="4dp"> <!-- Opcional: añade una sombra sutil -->

        <TextView
            android:id="@+id/tvReferenciasTexto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="De aquí sacamos la información para hacer nuestro proyecto..."
            android:textColor="#000000"
            android:textSize="16sp"
            android:gravity="center_horizontal|center_vertical"
            android:lineSpacingMultiplier="1.2"/>

    </LinearLayout>

    <Button
        android:id="@+id/btnRegresarreferencias"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="16dp"
        android:backgroundTint="#9E9E9E"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:text="Regresar"
        android:textColor="#FFFFFF" />

</RelativeLayout>
